let img = document.querySelector(".imo");
let container = document.getElementById("container")

function phones(phone) {
    img.src = phone;
};

function colors(color) {
    container.style.background = color;
}